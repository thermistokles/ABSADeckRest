package com.lti.absadeck.sod.repository;

import java.util.List;

import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;
import javax.persistence.Query;
import javax.transaction.Transactional;

import org.springframework.stereotype.Repository;


@Repository
public class SODRepositoryImpl{// implements SODRepository {

	/*
	 * @PersistenceContext private EntityManager entityManager;
	 * 
	 * public void addSOD(Sod sod) {
	 * 
	 * entityManager.persist(sod);
	 * 
	 * }
	 * 
	 * @Transactional public List<Sod> fetch() { Query q =
	 * entityManager.createQuery("select ob from Sod as ob");
	 * System.out.println("The list is:" + q.getResultList()); return
	 * q.getResultList(); }
	 */

}
